import Link from "next/link";import { signIn } from "@/app/actions/auth";
export default async function Login({searchParams}:{searchParams:Promise<{error?:string;message?:string}>}){const{error,message}=await searchParams;return <main className="auth-page"><form className="auth-card" action={signIn}><Link className="wordmark dark" href="/">mytown<span>app</span></Link><p className="eyebrow">Dashboard access</p><h1>Welcome back.</h1><p className="form-copy">Sign in with the account from your business or chamber invitation.</p>{error?<p className="form-error">{error}</p>:null}{message?<p className="form-success">{message}</p>:null}<label>Email<input name="email" type="email" autoComplete="email" required/></label><label>Password<input name="password" type="password" autoComplete="current-password" minLength={8} required/></label><p className="form-copy"><Link href="/forgot-password">Forgot your password?</Link></p><button className="primary" type="submit">Sign in</button><p className="form-copy">New here? <Link href="/signup">Create an account</Link></p></form></main>}
