import { signIn } from "@/app/actions/auth";
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){const{error}=await searchParams;return <main className="auth-page"><form className="auth-card" action={signIn}><a className="wordmark dark" href="/">mytown<span>app</span></a><p className="eyebrow">Dashboard access</p><h1>Welcome back.</h1><p className="form-copy">Sign in with the account from your business or chamber invitation.</p>{error?<p className="form-error">{error}</p>:null}<label>Email<input name="email" type="email" autoComplete="email" required/></label><label>Password<input name="password" type="password" autoComplete="current-password" minLength={8} required/></label><button className="primary" type="submit">Sign in</button></form></main>}
