import Link from "next/link";
import { businessMetrics, demoOffers } from "@mytownapp/core";
import { ArrowUpRight, Plus } from "lucide-react";
import { DashboardShell } from "@/components/dashboard";
export default function BusinessDashboard(){const offer=demoOffers[0];return <DashboardShell kind="business" metrics={businessMetrics}><div className="dashboard-grid"><section className="panel feature"><div className="panel-heading"><div><p className="eyebrow">Current offer</p><h2>{offer.title}</h2></div><span className="status">Live</span></div><p>{offer.description}</p><div className="offer-stats"><span><b>{offer.saves}</b> saves</span><span><b>{offer.redemptions}</b> redemptions</span></div><button className="primary">Manage offer <ArrowUpRight size={17}/></button></section><section className="panel"><div className="panel-heading"><div><p className="eyebrow">Quick start</p><h2>Keep your listing fresh</h2></div></div><div className="checklist"><span className="done">✓</span><p><b>Add business details</b><small>Your basics are ready.</small></p><span>100%</span></div></section></div><section className="panel activity"><div className="panel-heading"><div><p className="eyebrow">Recent activity</p><h2>Your local momentum</h2></div><Link className="secondary" href="/business/offers/new"><Plus size={16}/> New offer</Link></div>{["Offer saved by a neighbor","Birch & Main added to favorites","Offer redeemed in store"].map((x,i)=><div className="activity-row" key={x}><div className="activity-icon">{i===2?"✓":"♡"}</div><span><b>{x}</b><small>{i+1} hour{i?"s":""} ago</small></span><strong>{i===2?"Redeemed":"Engagement"}</strong></div>)}</section></DashboardShell>}
