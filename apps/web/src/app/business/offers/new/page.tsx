import{createOffer}from"@/app/actions/resources";import{ResourceForm}from"@/components/resource-form";import{requireRole}from"@/lib/auth";
export default async function NewOffer({searchParams}:{searchParams:Promise<{error?:string}>}){const[{error},auth]=await Promise.all([searchParams,requireRole(["business_admin","business_editor","platform_admin"])]);const businesses=auth.demo?[{id:"b1",name:"Birch & Main"}]:await auth.supabase!.from("businesses").select("id,name").then(r=>r.data??[]);return <ResourceForm title="Create an offer" eyebrow="Business dashboard" action={createOffer} error={error}><label>Business<select name="businessId" required>{businesses.map(b=><option value={b.id} key={b.id}>{b.name}</option>)}</select></label><label>Offer title<input name="title" required maxLength={90}/></label><label>Description<textarea name="description" rows={4}/></label><div className="field-grid"><label>Starts<input name="startsAt" type="datetime-local" required/></label><label>Ends<input name="endsAt" type="datetime-local" required/></label></div></ResourceForm>}
