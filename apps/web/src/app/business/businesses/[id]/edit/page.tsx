import { updateBusiness } from "@/app/actions/resources";
import { ResourceForm } from "@/components/resource-form";
import { requireRole } from "@/lib/auth";
import { redirect } from "next/navigation";
export default async function EditBusiness({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{error?:string}>}){const[{id},{error},auth]=await Promise.all([params,searchParams,requireRole(["business_admin","business_editor","platform_admin"])]);if(!auth.demo&&!auth.memberships.some(m=>m.business_id===id||m.role==="platform_admin"))redirect("/unauthorized");const business=auth.demo?{id:"b1",name:"Birch & Main",category:"Shopping",description:"Thoughtful home goods and gifts, chosen locally.",address:"18 Main Street",phone:"",website:""}:await auth.supabase!.from("businesses").select("id,name,category,description,address,phone,website").eq("id",id).single().then(r=>r.data);if(!business)redirect("/business");return <ResourceForm title="Edit business profile" eyebrow="Business dashboard" action={updateBusiness} error={error} description="Keep the information residents see in the mobile app accurate and useful." submitLabel="Save changes"><input type="hidden" name="id" value={business.id}/><label>Business name<input name="name" required maxLength={120} defaultValue={business.name}/></label><label>Category<input name="category" required maxLength={80} defaultValue={business.category}/></label><label>Description<textarea name="description" rows={5} maxLength={800} defaultValue={business.description}/></label><label>Address<input name="address" required maxLength={240} defaultValue={business.address}/></label><div className="field-grid"><label>Phone<input name="phone" type="tel" defaultValue={business.phone??""}/></label><label>Website<input name="website" type="url" defaultValue={business.website??""}/></label></div></ResourceForm>}
