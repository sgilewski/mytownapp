import Link from "next/link";import { signUp } from "@/app/actions/auth";
export default async function Signup({searchParams}:{searchParams:Promise<{error?:string}>}){const{error}=await searchParams;return <main className="auth-page"><form className="auth-card" action={signUp}><Link className="wordmark dark" href="/">mytown<span>app</span></Link><p className="eyebrow">Create your account</p><h1>Join your town.</h1><p className="form-copy">Use the same email address where you received your business or chamber invitation.</p>{error?<p className="form-error">{error}</p>:null}<label>Full name<input name="fullName" autoComplete="name" required/></label><label>Email<input name="email" type="email" autoComplete="email" required/></label><label>Password<input name="password" type="password" autoComplete="new-password" minLength={8} required/></label><button className="primary" type="submit">Create account</button><p className="form-copy">Already registered? <Link href="/login">Sign in</Link></p></form></main>}
