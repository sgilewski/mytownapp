import{inviteBusiness}from"@/app/actions/resources";import{ResourceForm}from"@/components/resource-form";import{requireRole}from"@/lib/auth";
export default async function NewInvitation({searchParams}:{searchParams:Promise<{error?:string}>}){const[{error},auth]=await Promise.all([searchParams,requireRole(["chamber_admin","platform_admin"])]);const chambers=auth.demo?[{id:"c1",name:"Hillside Chamber"}]:await auth.supabase!.from("chambers").select("id,name").then(r=>r.data??[]);return <ResourceForm title="Invite a business" eyebrow="Grow your local network" action={inviteBusiness} error={error}><label>Chamber<select name="chamberId" required>{chambers.map(c=><option value={c.id} key={c.id}>{c.name}</option>)}</select></label><label>Business owner email<input name="email" type="email" autoComplete="email" required/></label><p className="form-copy">The invitation will remain active for seven days. Email delivery is connected in the deployment slice.</p></ResourceForm>}
