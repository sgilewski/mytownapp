import{createEvent}from"@/app/actions/resources";import{ResourceForm}from"@/components/resource-form";import{requireRole}from"@/lib/auth";
export default async function NewEvent({searchParams}:{searchParams:Promise<{error?:string}>}){const[{error},auth]=await Promise.all([searchParams,requireRole(["chamber_admin","chamber_editor","platform_admin"])]);const chambers=auth.demo?[{id:"c1",name:"Hillside Chamber"}]:await auth.supabase!.from("chambers").select("id,name").then(r=>r.data??[]);return <ResourceForm title="Add an event" eyebrow="Chamber dashboard" action={createEvent} error={error}><label>Chamber<select name="chamberId" required>{chambers.map(c=><option value={c.id} key={c.id}>{c.name}</option>)}</select></label><label>Event title<input name="title" required maxLength={120}/></label><label>Description<textarea name="description" rows={4}/></label><label>Venue<input name="venue" required/></label><div className="field-grid"><label>Starts<input name="startsAt" type="datetime-local" required/></label><label>Recurrence<input name="recurrence" placeholder="Example: Every Friday in August"/></label></div></ResourceForm>}
