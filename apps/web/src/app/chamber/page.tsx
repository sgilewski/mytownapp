import { chamberMetrics, demoEvents } from "@mytownapp/core";
import { CalendarPlus, MailPlus } from "lucide-react";
import { DashboardShell } from "@/components/dashboard";
export default function ChamberDashboard() { return <DashboardShell kind="chamber" metrics={chamberMetrics}><div className="dashboard-grid"><section className="panel feature chamber-feature"><div className="panel-heading"><div><p className="eyebrow">Community pulse</p><h2>Hillside is showing up.</h2></div></div><p>Member activity is up 14% over the last 30 days. Keep the momentum going with a new business invite.</p><button className="primary"><MailPlus size={17}/> Invite a business</button></section><section className="panel"><div className="panel-heading"><div><p className="eyebrow">Upcoming</p><h2>Events at a glance</h2></div><button className="secondary"><CalendarPlus size={16}/> Add event</button></div>{demoEvents.map((event)=><div className="event" key={event.id}><time><b>{new Date(event.startsAt).toLocaleDateString("en-US",{month:"short"}).toUpperCase()}</b><strong>{new Date(event.startsAt).getDate()}</strong></time><span><b>{event.title}</b><small>{event.venue}</small></span></div>)}</section></div><section className="panel activity"><div className="panel-heading"><div><p className="eyebrow">Business network</p><h2>Participation</h2></div><a href="#businesses">View all businesses →</a></div>{[["Birch & Main","Active","4 offers"],["Foundry Coffee","Invited","Sent yesterday"],["Trailhead Outfitters","Active","2 offers"]].map((row)=><div className="business-row" key={row[0]}><div className="avatar">{row[0].split(" ").map(x=>x[0]).join("").slice(0,2)}</div><b>{row[0]}</b><span className={row[1]==="Active"?"status":"status pending"}>{row[1]}</span><small>{row[2]}</small></div>)}</section></DashboardShell>; }
