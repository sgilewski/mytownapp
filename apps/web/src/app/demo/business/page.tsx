import Link from "next/link";
import { Plus } from "lucide-react";
import { businessMetrics, demoBusinesses, demoOffers } from "@mytownapp/core";
import { DashboardShell } from "@/components/dashboard";
export default function BusinessDemo(){return <DashboardShell kind="business" metrics={businessMetrics}><section className="demo-banner"><b>Interactive dashboard preview</b><span>Sample data is shown here. Sign in to manage a real business workspace.</span><Link href="/login">Dashboard sign in</Link></section><section className="panel activity"><div className="panel-heading"><div><p className="eyebrow">Your businesses</p><h2>A clear view of local momentum</h2></div><Link className="secondary" href="/login"><Plus size={16}/> New offer</Link></div>{demoBusinesses.map(b=><div className="business-row" key={b.id}><div className="avatar">{b.name.split(" ").map(x=>x[0]).join("").slice(0,2)}</div><b>{b.name}</b><span className="status">published</span><small>{b.category}</small></div>)}</section><section className="panel activity"><div className="panel-heading"><div><p className="eyebrow">Offers</p><h2>Scheduled and active</h2></div></div>{demoOffers.map(o=><div className="activity-row" key={o.id}><div className="activity-icon">%</div><span><b>{o.title}</b><small>{o.status}</small></span><strong>{o.redemptions} redemptions</strong></div>)}</section></DashboardShell>}
