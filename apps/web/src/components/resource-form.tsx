import type{ReactNode}from"react";
export function ResourceForm({title,eyebrow,action,children,error}:{title:string;eyebrow:string;action:(formData:FormData)=>void|Promise<void>;children:ReactNode;error?:string}){return <main className="form-page"><form className="resource-form" action={action}><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p className="form-copy">Save this as a draft. You can review and publish it from the dashboard.</p>{error?<p className="form-error">{error}</p>:null}{children}<div className="form-actions"><a href="../..">Cancel</a><button className="primary" type="submit">Save draft</button></div></form></main>}
