import type{ReactNode}from"react";
export function ResourceForm({title,eyebrow,action,children,error,description="Save this as a draft. You can review and publish it from the dashboard.",submitLabel="Save draft"}:{title:string;eyebrow:string;action:(formData:FormData)=>void|Promise<void>;children:ReactNode;error?:string;description?:string;submitLabel?:string}){return <main className="form-page"><form className="resource-form" action={action}><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p className="form-copy">{description}</p>{error?<p className="form-error">{error}</p>:null}{children}<div className="form-actions"><a href="../..">Cancel</a><button className="primary" type="submit">{submitLabel}</button></div></form></main>}
